<span id="wishlist_badge" class="absolute -left-1 -top-1 grid size-5 place-items-center rounded-full bg-rose-500 text-xs text-white transition-opacity <%= "opacity-0" if count.zero? %>" aria-live="polite"><span aria-hidden="true"><%= count %></span><span class="sr-only"><%= count %> منتجات في المفضلة</span></span>
